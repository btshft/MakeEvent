@model IEnumerable<MakeEvent.Web.Models.Organization.EventMvcViewModel>
@{
    Layout = "~/Views/Shared/_OrgLayout.cshtml";
}
<div class="main-page">
    <p class="col-md-offset-1">
        <button class="btn indigo darken-1 btn-sm btn-default">
            @Html.ActionLink("Create New", "Create")<span class="glyphicon glyphicon-plus"></span>
        </button>
    </p>
    <div class="col-md-offset-1">
        <table class="table highlight">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ShortDescripton)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(model => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(model => item.ShortDescripton)
                    </td>
                    <td>
                        <button class="btn  blue darken-3 btn-sm btn-default">
                            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) <span class="glyphicon glyphicon-pencil"></span>
                        </button>
                        <button class="btn deep-purple lighten-1  btn-sm btn-default">
                            @Html.ActionLink("Details", "Details", new { id = item.Id }) <span class="glyphicon glyphicon-list"></span>
                        </button>
                        <button class="btn pink lighten-1 btn-sm btn-default">
                            @Html.ActionLink("Delete", "Delete", new { id = item.Id }) <span class="glyphicon glyphicon-remove"></span>
                        </button>
                    </td>
                </tr>
            }

        </table>
    </div>
</div>